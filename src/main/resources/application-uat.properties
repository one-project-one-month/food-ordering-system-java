spring.datasource.url=jdbc:mysql://${DB_HOST:mysql-db}:${DB_PORT:3306}/${DB_NAME}?createDatabaseIfNotExist=true
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#JavaMailSender_Config
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:minzayarmaung2002@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vgklvkranagneybo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000

#Flyway_log
logging.level.org.flywaydb=INFO

jwt.secret=ZP4g8Nj6QbXWYr2NvG2kXsKcD1HLqEyV

#redis
spring.data.redis.host=${REDIS_HOST:redisdb}
spring.data.redis.port=${REDIS_PORT:6379}

cloud.aws.credentials.access-key=minio
cloud.aws.credentials.secret-key=minio123
cloud.aws.s3.bucket=my-bucket
cloud.aws.region.static=us-east-1
cloud.aws.s3.endpoint=http://minio:9000