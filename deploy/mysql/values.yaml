primary:
  nodeSelector:
    topology.kubernetes.io/zone: ap-southeast-1a

  persistence:
    enabled: true
    size: 1Gi
    storageClass: gp2

  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 1Gi

